pluginManagement {
    repositories {
        jcenter()
        google()
        mavenCentral()
        maven { url = uri("https://plugins.gradle.org/m2/") }
    }
}

buildCache {

}

rootProject.name = "Carcassonne"
